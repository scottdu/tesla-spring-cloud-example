spring:
  application:
    name: tesla-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liaokailin/config-repo
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: true
    fetch-registry: true

